configs:
  params:
    # Disable HTTPS redirect — Traefik handles TLS termination at the ingress
    server.insecure: "true"
    # Sub-path routing: ArgoCD is served at /argocd via Traefik IngressRoute
    server.rootpath: "/argocd"
  cm:
    # Kind/NodePort clusters: Traefik never populates Ingress ADDRESS — mark Ingress as always Healthy
    resource.customizations.health.networking.k8s.io_Ingress: |
      hs = {}
      hs.status = "Healthy"
      hs.message = "Ingress"
      return hs
    # Istio injects caBundle into webhook configs at runtime — ignore the diff globally
    resource.customizations.ignoreDifferences.admissionregistration.k8s.io_ValidatingWebhookConfiguration: |
      jsonPointers:
      - /webhooks/0/clientConfig/caBundle
    resource.customizations.ignoreDifferences.admissionregistration.k8s.io_MutatingWebhookConfiguration: |
      jsonPointers:
      - /webhooks/0/clientConfig/caBundle
      - /webhooks/1/clientConfig/caBundle
      - /webhooks/2/clientConfig/caBundle
      - /webhooks/3/clientConfig/caBundle

server:
  resources:
    limits:
      memory: 256Mi
      cpu: 250m
    requests:
      memory: 128Mi
      cpu: 100m
